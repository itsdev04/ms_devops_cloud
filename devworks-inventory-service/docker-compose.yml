services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_DATABASE: inventory_service
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3308:3306" # Maps container's 3306 to your local machine's 3307